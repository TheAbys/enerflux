version: '3'

tasks:
  run:
    desc: Run application
    cmds:
      - set -a && source .env && set +a && go run ./cmd/api

  test:
    desc: Run all tests
    cmds:
      - go test ./...

  build:
    desc: Build binary
    cmds:
      - go build -o bin/enerflux ./cmd/api